# Express + TypeScript + SQLite CRUD 

This is a minimal, production‑ready ExpressJS backend in **TypeScript** with a simple **SQLite** database for persistence. 
It exposes a classic CRUD API for a single resource (`items`) and includes request validation, filtering, sorting, pagination, and a clean project structure.

This document will be updated along with the development of this prod :D